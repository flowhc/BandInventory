import { StyleSheet } from 'react-native';

export const textStyles = StyleSheet.create({
header: {
    color: '#ffffff',
    fontSize: 24,
    textAlign: 'center',    
    },
title: {
    color: '#ffffff',
    fontSize: 24,
    },
buttonText: {
    color: 'red',
    fontWeight: 'bold',
    textAlign: 'center',
    fontSize: 18
    },
}); 